# Exact Boost.UT names preserved from ec5255d4eab820b97f8fb76d95381354b40900d8.
# One name per line; additions are tracked separately by the layout verifier.
FE_Nothing does not constrain the active phase trace in 2D and 3D
MPI finalization-query failure enters the fatal path
MPI initialization-query failure enters the fatal path
MPI query and agreement failures are fatal
RFC 3629 boundary scalar encodings are accepted
RFC 3629 rejects every malformed UTF-8 sequence class
Rift reports its configured semantic version
UTF-8 names use unsigned-byte canonical ID and callback order
a communicator outside MPI_COMM_WORLD has no supported run provenance
a construction-time validator rejects incompatible interface models
a deterministic model predicts multiple short collective state sequences in 2D and 3D
a finite level-set edit on only the nonroot or middle rank advances one agreed revision
a graph copy owns independent descriptor storage and shared run provenance
a graph may contain more than two phases
a graph may contain one phase and no interfaces
a graph rejects a phase ID outside its local descriptor array
a graph retains its run after the source communicator and handle are released
a local compatibility exception is rethrown unchanged
a logical rejection consumes no collective transaction identity in 2D and 3D
a new store has no previous accepted snapshot in 2D and 3D
a new store publishes a zero-initialized accepted snapshot in 2D and 3D
a non-root run-ID participant accepts the broadcast sequence
a null communicator is rejected without entering a collective
a private-base child retains the accepted root rather than its immediate private base
a published sibling makes another private sibling stale in 2D and 3D
a runtime graph must contain at least one phase
a state store is a movable nonassignable collective authority
a supported root cannot hide an unsupported communicator member
a three-phase cycle preserves each declared orientation
abandoning a trial discards its mutable values in 2D and 3D
actual draft and finalize post-agreement failures use retained fatal policy
agreed communicator classifications produce structured errors
allocation failure between compatibility callbacks is fatal
an absent phase field name returns no identity in 2D and 3D
an already published snapshot cannot be published again in 2D and 3D
an immutable snapshot outlives its state store in 2D and 3D
an independent public lookup model enumerates every bounded short action sequence
an interface graph requires an explicit compatibility check
an interface may not join a phase to itself
an interface must name two phases in the graph
an interface reports a missing plus phase
an interface resolves from its unordered incident phase pair
artificial-cell guards never evaluate cell accessors
callback bad_alloc is rethrown as the original user exception
canonical JSON escapes every ASCII control byte
canonical input allocation failure is fatal before exact agreement
canonical interface ordering compares every logical field
canonical serialization escapes every JSON control character
canonical serialization is independent of configuration insertion order
canonical serialization separates multiple interfaces
classification-agreement failure is fatal
cleanup finalization-query failure enters the fatal path
cleanup initialization-query failure enters the fatal path
cleanup skips MPI resources outside the active lifecycle
collective agreement advances a lagging local allocator only when needed
collective diagnostics preserve structured keys and exactly deduplicate
collective exception agreement returns the communicator disjunction
collective state descriptors and partitions agree in 2D and 3D
collective storage allocation failures enter the fatal path
communicator classification covers every logical relationship
communicator comparison failure is fatal
communicator duplication failure enters the fatal MPI path
communicator duplication failure is fatal
communicator query and local allocation failures are fatal
communicator rank zero maps to one MPI world origin
communicator-free failure enters the fatal path
communicator-kind query failure enters the fatal MPI path
communicator-kind validation accepts an intracommunicator and rejects an intercommunicator
compatibility callbacks receive oriented arguments in canonical interface order
complete construction and production paths preserve every 2D and 3D identity
construction reports independent configuration errors together
control allocation failure enters the fatal path without a collective cleanup
create invokes a fatal handler when duplication fails
create rejects a communicator whose origin is outside MPI_COMM_WORLD
create releases its duplicate after collective sequence exhaustion
discard rejects an unknown snapshot identity in 2D and 3D
discard removes a private snapshot but cannot remove accepted state in 2D and 3D
disjoint partial phase masks synchronize multicomponent FE Nothing in 2D and 3D
distributed multilevel closure reaches the serial least fixed point in 2D and 3D
distributed reconstruction failures are fatal before another collective
distributed schemas, epochs, diagnostics, and ghost FE indices agree in 2D and 3D
duplicate cleanup failure enters the fatal MPI path
duplicate interface names are rejected
duplicate phase names are rejected
each phase field group owns an independent DoFHandler in 2D and 3D
edge orientation does not depend on phase insertion order
equal law keys do not turn one pair into two edges
equal local phase IDs from separate graphs are not interchangeable
equal local phase IDs from separate runs are not interchangeable
every MPI failure while deriving the origin enters the fatal path
every active cell uses its field group's uniform degree in 2D and 3D
every canonical graph string field requires UTF-8
every collective MPI status failure enters the fatal path
every communicator member must belong to the current world
every state protocol stage reports atomic identity exhaustion in 2D and 3D
every state transition error code has stable allocation-free text
every supplied interface requires a compatibility callback
every three-phase cycle declaration permutation has one canonical graph
exact byte agreement distinguishes equal and unequal payloads
field access and sealing reject an expired store in 2D and 3D
field groups must refer to a phase in the graph in 2D and 3D
field identities cannot be resolved through the wrong phase in 2D and 3D
foreign distributed drafts retain last mesh ownership until matched release in 2D and 3D
graph construction reports exhaustion of its finite instance-ID space
graph-ID collective failure enters the fatal MPI path
group translation and cleanup failures are fatal
hanging-face support reaches the exact 2D and 3D least fixed point
identity exhaustion leaves pin capacity and transient retention atomic
independent field-schema errors are collected in 2D and 3D
independent level-set schema errors are collected in 2D and 3D
interface names may not be empty
interface operator keys may not be empty
invalid regional schemas are rejected during 2D and 3D finalization
level-set revisions use exact binary64 representations and retry exhaustion
local field lookup validates complete 2D and 3D provenance
logical MPI lifecycle values remain recoverable errors
logical lifecycle and null values are recoverable
malformed gathered diagnostics are fatal
maximum numeric identifiers cannot alias valid descriptors
mesh ID MPI failures are fatal
mesh ID exhaustion remains recoverable
mesh cleanup MPI failures are fatal
mesh cleanup skips inactive MPI and otherwise releases once
mesh sequences stop before the reserved exhaustion sentinel
mesh snapshots consume and immutably own 2D and 3D triangulations
mesh snapshots retain run and communicator lifetime in 2D and 3D
missing interfaces do not resolve by name or phase pair
moving a store preserves live transaction authority in 2D and 3D
moving a transaction transfers its sole mutable authority in 2D and 3D
multilevel hanging support needs multiple global growth iterations in 2D and 3D
new retention operations agree before mutation across two and three ranks
null 2D and 3D triangulations are rejected collectively
one owner-local support object is shared by all fields of a phase
one participant outside MPI_COMM_WORLD rejects the communicator on every rank
one phase may not declare the same field-group name twice in 2D or 3D
phase and interface names resolve to stable numeric ids
phase cells outside the support envelope own no DoFs in 2D and 3D
phase names may not be empty
phase physics keys may not be empty
phase support is exactly once per represented phase and may be empty
phase-graph exhaustion retains its domain-specific diagnostic
pin unpin and discard reserve no identity component on success or failure
pinning is bounded, idempotent, and protects private snapshots in 2D and 3D
post-collective snapshot allocation failure is fatal
post-duplication control allocation failure is immediately fatal
preparation propagates exhaustion and treats local allocation as fatal
private control cleanup before MPI initialization does not free a communicator
private gateway exposes the retained run and controlled mutable mesh
publication evicts old metadata while external handles survive in 2D and 3D
publishing a candidate advances the accepted epoch and retains the previous state in 2D and 3D
publishing a pin frees capacity and leaves a transient sibling registered but stale
rank translation allocation failure enters the fatal path
rank-local distributed rebuild failure never frees collectively
rank-local snapshot allocation failure never frees collectively
refined-away support cell identities are inactive in 2D and 3D
regional entries join the final layout in stable order in 2D and 3D
regional scalars preserve every binary64 class and sibling isolation
regional setter errors are atomic and reserve no identity component
regional state agrees exactly and preserves owner-independent caches
regional state preserves exact representations in 2D and 3D
regional values survive retention transitions and every owning store lifetime
registry snapshots outlive the caller communicator handle in 2D and 3D
rejected drafts consume epochs and complete provenance rejects a foreign registry
retention errors preserve precedence and extreme capacities do not overflow
root and nonroot participants form the same origin-plus-sequence representation
run construction reports that MPI is already finalized
run construction reports that MPI is not initialized
run-ID broadcast failure enters the fatal MPI path
run-first store agreement rejects alternating congruent mesh contexts in 2D and 3D
sealing a trial leaves its immutable base unchanged in 2D and 3D
sealing is rejected after a transaction becomes inactive in 2D and 3D
separately consumed equal-topology meshes have distinct 2D and 3D identities
space construction rejects wrong-mesh and cross-graph provenance
space identity MPI failures are fatal
space payload MPI and count failures are fatal
space payload framing preserves arbitrary strings and rejects malformed bytes
state MPI status translation is fatal only for non-success
state identity bundle exhaustion never partially advances another sequence
state identity bundle success encodes an origin and advances only selected sequences
state store and snapshot IDs stay unique across disjoint, world, and reversed communicators
state transition errors are fixed-size allocation-free values
storage kind and empty-mesh classifications agree before ID allocation
store moves preserve retention and external handles outlive every removal
strong ids compare only with the same semantic id type
successful ownership exposes and releases the duplicated communicator
support envelopes reject cells outside the background mesh in 2D and 3D
support-envelope rebuilds receive a new epoch in 2D and 3D
the initial graph has at most one edge per unordered phase pair
the level-set field group covers the complete 2D and 3D background mesh
the level-set snapshot identity changes only with level-set values in 2D and 3D
the maximum finite epoch sequence is an exhaustion sentinel
the per-origin run sequence reserves its maximum representation for exhaustion
the private ID allocator saturates instead of wrapping
the state store reports its finalized space epoch in 2D and 3D
two phases retain disjoint masks for multicomponent fields
two-rank hanging closure returns requests to 2D and 3D cell owners
unequal law keys remain one conflicting composite-law pair
unknown snapshot identities are rejected before mutation or publication in 2D and 3D
unpin promotes immediately and active bases survive lookup eviction in 2D and 3D
unrepresentable collective byte counts enter the fatal path
unsafe space failures use the fatal handler retained by the run
valid multibyte spellings remain byte-exact and unnormalized
